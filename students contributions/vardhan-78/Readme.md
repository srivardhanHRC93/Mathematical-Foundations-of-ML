

Features:
show_id	type	title	director	cast	country	date_added	release_year	rating	duration	listed_in	description

Find the algorithm which can be applied on this dataset?
The algorithm which we can use for this dataset is clustering(unsupervised learning),since the given dataset is not given in tabular form , so to understand it we can create a table based on genre duration or release year .  

Clean the dataset.

Find input and output features.
The unput features are type, title, director,cast , country,date_added,release_year,duration.
OUTPUT:
If you are predicting the rating you can take the output as rating which is on y-axis.
